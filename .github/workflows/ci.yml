name: CI
on:
  pull_request:
    branches: [main]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r requirements-dev.txt
      - run: ruff check .
      - run: pytest -q
  evals:
    if: contains(github.event.pull_request.labels.*.name, 'evals')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r requirements-dev.txt
      - run: python evals/run.py --golden evals/golden/golden_v0.jsonl --report evals/output/report.md
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY_EVALS }}
